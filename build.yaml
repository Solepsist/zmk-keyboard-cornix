# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
---
include:
  # ZMK v0.3.x / Zephyr 3.5: use board id nice_nano_v2 (nice!nano v2 / same UF2 layout as common Pro Micro dongles).
  # Drop nrf52840-nosd / studio-rpc snippets — those targets Zephyr 4.1+ toolchains; add CONFIG_* in shield conf if needed.
  - board: nice_nano_v2
    shield: cornix_dongle_adapter cornix_dongle_eyelash dongle_display
    artifact-name: cornix_dongle_nosd
  # dongle screen not yet supports zephyr4
  # - board: seeeduino_xiao_ble
  #   shield: cornix_dongle_adapter dongle_screen
  #   snippet: studio-rpc-usb-uart
  #   artifact-name: cornix_dongle_zds_see
  # - board: nice_nano
  #   shield: cornix_dongle_adapter dongle_screen
  #   snippet: studio-rpc-usb-uart
  #   artifact-name: cornix_dongle_zds_nano
  - board: cornix_left
    # shield: cornix_indicator
    artifact-name: cornix_left_default_nosd

  - board: cornix_ph_left
    # shield: cornix_indicator
    artifact-name: cornix_left_for_dongle_nosd

  - board: cornix_right
    # shield: cornix_indicator
    artifact-name: cornix_right_nosd

  # Right half has CONFIG_ZMK_USB=n; do not use Studio RPC snippet here.
  - board: cornix_right
    shield: settings_reset
    artifact-name: cornix_reset

  - board: nice_nano_v2
    shield: settings_reset
    artifact-name: reset_nicenano_nosd
